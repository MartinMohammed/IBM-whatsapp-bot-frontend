import type { IClientStoredContact } from "chat-app-server";

/** This function is responsible for fetching all contacts from the backend. The idea is at possible we only want to store as much messages as needed. */
// TODO
export function fetchAllContacts(): IClientStoredContact[]{
    // Use the browser fetch api. 
    return []
}